import { AvatarhoverDirective } from './avatarhover.directive';

describe('AvatarhoverDirective', () => {
  it('should create an instance', () => {
    const directive = new AvatarhoverDirective();
    expect(directive).toBeTruthy();
  });
});
